[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "terraform_oncall_assistant"
version = "0.1.0"
dependencies = [
    "aiohttp>=3.8.0"
]